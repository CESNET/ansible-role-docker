[Unit]
Description=Restart unhealthy docker containers
After=docker.service
Wants=docker.service

[Service]
ExecStart=/usr/local/bin/docker-unhealthy-autorestarter
User=docker-unhealthy-autorestarter
Group=docker
Type=simple
Restart=always

[Install]
WantedBy=multi-user.target
